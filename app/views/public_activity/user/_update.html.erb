Updated the

<% if activity.trackable %>
	<%=activity.trackable.user_type.eql?("employee") ? "Employee" : "Contact Point"%>
  	<b><%=activity.trackable.firstname%></b>
	<%#= link_to activity.trackable.firstname, activity.trackable %>
<% else %>
  which has since been removed
<% end %>