<body>
	<div class="container">
		<div class= "row row-offcanvas row-offcanvas-right">	
			<div class= "col-xs-12 col-sm-9">
				<div class="row">
	    			<div class="col-lg-6">
						<fieldset>
							<%= form_tag (roles_path) ,:class => "class_roleForm"  do %>
								
								<%= fields_for :company, @company do |f| %>
					   		
									<%= f.fields_for :roles, @role do |builder| %>
					   					<%= render 'role_fields', f: builder %>
	  								<% end %>
									
	  								<%#= link_to_add_fields '<i class="icon-plus" style = "margin: -67px 0px 0px 330px;"></i>'.html_safe,f, :roles %>
									<%= render 'form_buttons' ,f: f %>
	  			      	  	   <% end %>
								
							<% end %>
						</fieldset>
					</div><!--/span--> 
					<div class="col-lg-3"></div>
	      		</div><!--/row-->
	  		</div>
  		</div>
	</div><!--/.container-->
</body>

